# Language setting
language: node

services:
  - elasticsearch

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - echo "true"

