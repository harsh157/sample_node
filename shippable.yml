# Language setting
language: node_js

services:
  - mysql
  - neo4j
  - rabbitmq
  - redis
  - rethinkdb
  - selenium

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - echo $ABC


    

    

    
  
  

