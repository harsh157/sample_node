# Language setting
language: node_js

services:
  - elasticsearch

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - echo "true"

