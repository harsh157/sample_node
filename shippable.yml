# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU
    - ABC="$SHIPPABLE_POSTGRES_BINARY"
    - SHIPPABLE_POSTGRES_CMD="sudo -u postgres $SHIPPABLE_POSTGRES_BINARY"

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - echo ABC


    

    

    
  
  

