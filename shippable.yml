# Language setting
language: ruby

services:
  - elasticsearch

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE

build:

  pre_ci_boot:
    image_name: shippable/minv2
    image_tag: latest
  ci:
    - ps -eax
    - ls -al

  post_ci:
    - echo "true"

