# Language setting
language: node_js

services:
  - postgres
  - couchdb
  - elasticsearch
  - memcached
  - mongodb

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - echo $ABC


    

    

    
  
  

